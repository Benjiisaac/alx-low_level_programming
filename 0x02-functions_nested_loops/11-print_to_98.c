#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - prints all natural numbers from n to 98
 * @n: number input
 * Return: 0 if wrong 
 */

void print_to_98(int n)
{
	if (n <= 98)
	{
		_putchar(n + '0');
		_putchar(,);
		_putchar( );
		n++;
	}
	else
		printf("this is a dud");
}
